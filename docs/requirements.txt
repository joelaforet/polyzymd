# Documentation build dependencies
sphinx>=6.0.0
sphinx-rtd-theme>=1.0.0
myst-parser>=1.0.0
sphinx-copybutton>=0.5.0

# Required for autodoc to import config modules
pydantic>=2.0.0
click>=8.0.0
pyyaml>=6.0

# Note: Heavy dependencies (openmm, openff, rdkit, mdtraj, etc.) are mocked
# in conf.py via autodoc_mock_imports. This allows autodoc to generate API
# documentation without requiring CUDA/GPU libraries on Read the Docs.
