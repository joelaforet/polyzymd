# Notebook scaffolds

Do not copy notebooks from this directory into a project manually.

Run `polyzymd init --name my_project` to create the canonical CGSmiles polymer
authoring notebook under `my_project/notebooks/` and its matching
`my_project/generated_molecules/` output directory.
