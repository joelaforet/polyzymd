"""Result models for the {{ spec.name }} analysis."""

from __future__ import annotations

from pydantic import BaseModel


class {{ spec.class_name }}ReplicateResult(BaseModel):
    """Result from a single replicate of the {{ spec.name }} analysis."""

    replicate: int
    value: float
    n_frames: int
    example_parameter: str = ""


class {{ spec.class_name }}AggregatedResult(BaseModel):
    """Aggregated {{ spec.name }} result across replicates."""

    mean_value: float
    sem_value: float
    replicate_values: list[float]
    n_replicates: int
